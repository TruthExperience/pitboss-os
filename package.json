{
  "name": "pitboss-os",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*",
    "packages/types/shared"
  ],
  "engines": {
    "node": "18"
  },
  "scripts": {
    "dev": "pnpm --filter @pitboss/web dev",
    "build": "pnpm --filter @pitboss/web build",
    "lint": "pnpm --filter @pitboss/web lint"
  }
}
